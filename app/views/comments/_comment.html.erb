<%= form_for @comment = Comment.new, remote: true do |f| %>
  <%= f.hidden_field :recipe_id, value: recipe.id %>
  <%= f.text_field :content, class: "base-border-rounded form-control", placeholder: "Bạn nghĩ gì về món ăn này?", autocomplete: :off %>
<% end %>